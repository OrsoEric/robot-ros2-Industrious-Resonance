# Speech To Text

I need ASR Automatic Speech Recognition

Microphone array to get high quality audio, noise cancelling and direction detection

Wake Word, where the microphone listens for an activation word and wakes up

Speech to Text where it streams recognized in real time

# Facts

- I need the model not to be in the useless .cache folder, but to be in the project folder, otherwise it doesn't stay with the machine- 

## Virtual Environment

Python is incompetently designed. If python runs on a different absolute path (e.g. same project on laptop and desktop), they do not work, because of the gross incompetence of python, absolute paths are baked in, even with UV, so if I run them from two computers, even with multiple gigabytes of dependencies handled, it still won't work.

I need a virtual environment that is many GB in size for each project for each machine they run

# TESTS

## FREISA-GPT MCP Whisper

2025-10-27

Missing UV environment and models and dependencies

## RealtimeSTT

2025-10-27

D:\Data\Project\Project-LLM\RealtimeSTT

I have ToB environment, and little else apparently, missing dependencies

## Glados

This is an end to end assistent with STT and TTS

D:\Data\Project\Project-LLM\GLaDOS

2025-10-27
I do have UV env and model folder
test_stt.py miss loguru dependency




